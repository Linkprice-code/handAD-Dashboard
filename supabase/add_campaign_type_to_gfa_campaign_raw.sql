-- =========================================================
-- gfa_campaign_raw에 campaign_type(캠페인 목적) 컬럼 추가
-- ---------------------------------------------------------
-- 네이버 GFA 캠페인 Raw 리포트의 "캠페인 목적" 컬럼 값
-- (웹사이트 전환 / 인지도 및 트래픽 / 쇼핑 프로모션 / 카탈로그 판매 /
-- 동영상 조회 등)을 그대로 저장해서, "캠페인 유형별 성과" 표에 쓴다.
-- 예전에 업로드된 행에는 없을 수 있어 nullable로 둔다.
-- =========================================================

alter table gfa_campaign_raw
  add column if not exists campaign_type text;
